---
# OintOS keyboard defaults (upstream keyboard.conf example).
# NOTE: ckbcomp must be installed on the live system for layout previews.
xOrgConfFileName: "/etc/X11/xorg.conf.d/00-keyboard.conf"
convertedKeymapPath: "/lib/kbd/keymaps/xkb"

configure:
    kwin: false
    gnome: false
