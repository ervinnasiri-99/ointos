---
# OintOS default timezone (upstream locale.conf example; user changes it
# on the locale page; GeoIP json lookup may override the pin).
region: "America"
zone: "New_York"

geoip:
    style: "json"
    url: "https://geoip.kde.org/v1/calamares"
    selector: ""
